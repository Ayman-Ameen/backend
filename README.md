This is a simple package which enable to use the same code for multiple packages like numpy and torch. 
The current version support only numpy and pytorch. Please, extend it to contain Tensorflow, JAX and ... 